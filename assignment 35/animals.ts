// Define an array with pet animals
let pets: string[] = ['dog', 'cat', 'rabbit'];

// Loop through the array of pet animals
for (let pet of pets) {
    console.log(`A ${pet} would make a great pet.`);
}

// Additional statement outside the loop
console.log('Any of these animals would make a great pet!');