// Define the function make_shirt with parameters for size and message
function make_shirt(size, message) {
    console.log("The size of the shirt is ".concat(size, " and it says: '").concat(message, "'."));
}
// Call the fuction with specific size and message
make_shirt('Medium', 'TypeScript Rocks!');
