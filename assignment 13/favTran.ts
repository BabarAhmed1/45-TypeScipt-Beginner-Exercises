// An array storng examples of favorite cars.
const favoriteCars: string[] = ['Honda Civic', 'Toyota Corolla', 'Ford Mustang', 'Tesla Model S'];

// Iterating over the array to print statements about each car.
favoriteCars.forEach(car => {
    console.log(`I would like to own a ${car}.`);
});