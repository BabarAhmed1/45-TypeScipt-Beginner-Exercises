/* This program stores the names of a few friends in an array called names
and prints each person's name by accessing each element in the list, one 
at a time. */

const names: string [] = ['Shazil', 'Shanzeh', 'Zuhair', 'Shahzain'];

for (let i = 0; i < names.length; i++) {
    console.log(names[i]); 
}